import { AWPlugin } from '../../common/plugin';
export declare class AWContacts extends AWPlugin {
    create(contact: any): any;
    find(fields: any, options?: any): void;
    pickContact(): void;
}
