import {AWPlugin} from '../core/aw-plugin';

export class AWCamera extends AWPlugin {
    cleanup(onSuccess: () => void, onError: (message: string) => void) {
        // TODO
    }

    getPicture(cameraSuccess: (data: string) => void, cameraError: (message: string) => void, cameraOptions?: any) {
        // TODO
    }

    openGallery(options?: any) {
        // TODO
    }

    takePicture(options?: any) {
        // TODO
    }
}