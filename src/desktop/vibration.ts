import {AWPlugin} from '../core/aw-plugin';

export class AWVibration extends AWPlugin {

    constructor() {
        super(() => {
        }, () => {
        });
    }

    vibrate(time: number) {
        // TODO
    }
}